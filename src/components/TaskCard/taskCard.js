import { Link } from "react-router-dom";

export default function TaskCard() {
    return (
      <div>
        <Link to="/view">
        <p>This will be a card for a task</p>
        </Link>
      </div>
    );
  }
  