import { Link } from "react-router-dom";

export default function TaskCard() {
    return (
      <div>
        <Link to="/view">
        <p>Task Card Preview</p>
        </Link>
      </div>
    );
  }
  